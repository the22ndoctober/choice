import { Colors } from "@/client"

export const HeadLinksStyles = {
    wrapper: {
        color: Colors.dark,
        fontSize: "16px",
        alignItems: "center",
        px: "36px",
        py: "21px",
        columnGap: 1,
        cursor: "pointer",
    },
}
