"use client"

import { Box, Grid } from "@mui/material"

//Кольори
import { Colors } from "@/client"

const SaveTheWorld = ({}) => {
    return (
        <Box
            sx={{ width: "100%", minHeight: "70svh", background: Colors.paper }}
        >
            <Grid
                container
                sx={{
                    margin: "0 auto",
                    flexDirection: "column",
                    width: { xl: 1440, lg: 1368, xs: 360 },
                }}
            >
                {
                    //start code here
                }
            </Grid>
        </Box>
    )
}

export default SaveTheWorld
